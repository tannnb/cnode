export const author = state => state.author

export const userInfo = state => state.userInfo

export const countFlow = state => state.countFlow

export const pathUrl = state => state.pathUrl

export const accessToken = state => state.accessToken
